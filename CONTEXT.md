# Session 管理器

本機 Claude Code / Codex 對話 session 的檢視與接續工具。單一自包含 HTML 儀表板，供使用者快速找到並接續最近使用過的 session。

## Language

**接續快速區**：
儀表板頂部、不分專案的平面區塊，顯示全站最新 8 筆可接續的 session。永遠獨立於搜尋／篩選（固定錨點），且挑選時排除失效路徑的 session。
_Avoid_: 最近使用列表、recent list

**專案樹**：
儀表板主體區域，以檔案總管式縮排（展開圖示＋連接線）呈現「專案節點 → 路徑子節點 → 時間區間 → session」的階層關係，預設全部收合，供依專案查找舊 session 使用。
_Avoid_: 分群區、手風琴摺疊區、node-link 圖、tree view

**專案節點**：
專案樹中對應同一個 displayName（專案名稱）的頂層節點。若同一專案曾存在於多個磁碟路徑（例如搬移或複製過），該節點下會有多個路徑子節點。
_Avoid_: 叢集、cluster

**雜項**：
cwd 為使用者本機家目錄本身（而非任何專案資料夾）的 session 集合，在專案樹中是沒有路徑子節點的淺層分支。
_Avoid_: misc、隨手

**退而標題**：
當掃描不到可用的真實對話開場白時，退而以「資料夾名稱＋建立時間戳記」組成的替代標題。UI 上需以視覺警告（灰階／斜體）與真實標題明顯區隔。
_Avoid_: 假標題、預設標題

**失效路徑**：
session 記錄的 cwd，在產生儀表板當下已不存在於本機磁碟（專案被刪除，或本機沒有該路徑）。UI 需加視覺警告，且不列入接續快速區的挑選候選。
_Avoid_: 已刪除專案、無效路徑
